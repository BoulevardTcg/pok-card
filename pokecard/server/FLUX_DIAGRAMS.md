# 📊 Diagrammes de Flux - Backend BoulevardTCG

## 🔐 Flux d'Authentification

```
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ POST /api/auth/register
       │ { email, username, password }
       ▼
┌─────────────────────────────────┐
│  Backend - Route /auth/register │
└──────┬──────────────────────────┘
       │
       ├─► Valide les données (express-validator)
       ├─► Vérifie si email/username existe déjà
       ├─► Hash le mot de passe (bcrypt, 12 rounds)
       ├─► Crée l'utilisateur en DB (Prisma)
       └─► Crée le profil utilisateur
       │
       │ 201 Created
       ▼
┌─────────────┐
│  Frontend   │
└─────────────┘

┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ POST /api/auth/login
       │ { email, password }
       ▼
┌─────────────────────────────────┐
│  Backend - Route /auth/login    │
└──────┬──────────────────────────┘
       │
       ├─► Valide les données
       ├─► Trouve l'utilisateur (Prisma)
       ├─► Vérifie le mot de passe (bcrypt.compare)
       ├─► Génère Access Token (JWT, 15 min)
       ├─► Génère Refresh Token (JWT, 7 jours)
       └─► Stocke Refresh Token en DB
       │
       │ 200 OK
       │ { accessToken, refreshToken, user }
       ▼
┌─────────────┐
│  Frontend   │ (Stocke les tokens)
└─────────────┘

┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ GET /api/users/profile
       │ Header: Authorization: Bearer <accessToken>
       ▼
┌─────────────────────────────────┐
│  Middleware authenticateToken   │
└──────┬──────────────────────────┘
       │
       ├─► Extrait le token du header
       ├─► Vérifie la signature (JWT)
       ├─► Vérifie l'expiration
       └─► Ajoute req.user avec les infos
       │
       │ Token valide
       ▼
┌─────────────────────────────────┐
│  Route /users/profile           │
└──────┬──────────────────────────┘
       │
       ├─► Récupère req.user.userId
       ├─► Trouve l'utilisateur (Prisma)
       └─► Retourne le profil
       │
       │ 200 OK
       │ { user: { ... } }
       ▼
┌─────────────┐
│  Frontend   │
└─────────────┘
```

---

## 🛒 Flux d'Achat

```
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ 1. GET /api/products/:slug
       ▼
┌─────────────────────────────────┐
│  Backend - Route /products/:slug│
└──────┬──────────────────────────┘
       │
       ├─► Trouve le produit (Prisma)
       ├─► Inclut les images et variants
       └─► Retourne les détails + stock
       │
       │ 200 OK
       │ { product: { ..., variants: [{ stock: 10, ... }] } }
       ▼
┌─────────────┐
│  Frontend   │ (Affiche le produit)
└──────┬──────┘
       │
       │ 2. POST /api/checkout/create-session
       │ { items: [{ variantId, quantity }], customerEmail }
       ▼
┌─────────────────────────────────┐
│  Backend - Route /checkout/...  │
└──────┬──────────────────────────┘
       │
       ├─► Valide les données
       ├─► Vérifie les quantités (max 100/item, 500 total)
       ├─► Trouve les variants (Prisma)
       ├─► Vérifie le stock disponible
       ├─► Vérifie les prix
       ├─► Valide les URLs de redirection
       ├─► Crée la session Stripe
       └─► Retourne l'URL de paiement
       │
       │ 201 Created
       │ { sessionId, url: "https://checkout.stripe.com/..." }
       ▼
┌─────────────┐
│  Frontend   │ (Redirige vers Stripe)
└──────┬──────┘
       │
       │ 3. Redirection vers Stripe
       ▼
┌─────────────────────────────────┐
│        Stripe Checkout          │
└──────┬──────────────────────────┘
       │
       ├─► Utilisateur saisit ses infos de paiement
       ├─► Stripe valide la carte
       ├─► Stripe traite le paiement
       └─► Stripe redirige vers success_url
       │
       │ Paiement réussi
       ▼
┌─────────────────────────────────┐
│  Stripe Webhook (asynchrone)    │
└──────┬──────────────────────────┘
       │
       │ POST /api/checkout/webhook
       │ Header: stripe-signature
       │ Body: { type: "checkout.session.completed", ... }
       ▼
┌─────────────────────────────────┐
│  Backend - Webhook Handler      │
└──────┬──────────────────────────┘
       │
       ├─► Vérifie la signature Stripe
       ├─► Parse les items depuis metadata
       ├─► Démarre une transaction Prisma
       │   ├─► Pour chaque item :
       │   │   ├─► Re-vérifie le stock
       │   │   ├─► Re-vérifie le prix (depuis DB)
       │   │   ├─► Décrémente le stock (atomique)
       │   │   └─► Crée OrderItem
       │   ├─► Crée la commande (Order)
       │   └─► Commit la transaction
       │
       │ 200 OK
       │ { received: true }
       ▼
┌─────────────────────────────────┐
│  Base de Données                │
│  - Stock décrémenté             │
│  - Commande créée               │
└─────────────────────────────────┘

┌─────────────┐
│  Frontend   │ (Page de succès)
└──────┬──────┘
       │
       │ 4. GET /api/users/orders
       │ Header: Authorization: Bearer <token>
       ▼
┌─────────────────────────────────┐
│  Backend - Route /users/orders  │
└──────┬──────────────────────────┘
       │
       ├─► Authentifie l'utilisateur
       ├─► Trouve les commandes (Prisma)
       └─► Retourne les commandes
       │
       │ 200 OK
       │ { orders: [{ ... }] }
       ▼
┌─────────────┐
│  Frontend   │ (Affiche les commandes)
└─────────────┘
```

---

## 🔒 Flux de Sécurité

```
Requête entrante
       │
       ▼
┌─────────────────────────────────┐
│  CORS Middleware                │
│  - Vérifie l'origine            │
│  - Autorise ou bloque           │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Helmet Middleware              │
│  - Ajoute les headers sécurité  │
│  - CSP, HSTS, etc.              │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Validate Input Middleware      │
│  - Vérifie la taille du body    │
│  - Vérifie le Content-Type      │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Sanitize Input Middleware      │
│  - Nettoie les entrées          │
│  - Retire les caractères dangereux│
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Injection Protection           │
│  - Détecte les tentatives       │
│  - Bloque si suspect            │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Rate Limiting                  │
│  - Vérifie le nombre de requêtes│
│  - Bloque si dépassement        │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Secure Logging                 │
│  - Log sans données sensibles   │
└──────┬──────────────────────────┘
       │
       ▼
┌─────────────────────────────────┐
│  Route Handler                  │
│  - Traite la requête            │
└─────────────────────────────────┘
```

---

## 💾 Flux de Base de Données

```
┌─────────────────────────────────┐
│  Route Handler                  │
└──────┬──────────────────────────┘
       │
       │ Prisma Client
       ▼
┌─────────────────────────────────┐
│  Prisma ORM                     │
│  - Génère les requêtes SQL      │
│  - Protège contre les injections│
│  - Gère les relations           │
└──────┬──────────────────────────┘
       │
       │ SQL Query
       ▼
┌─────────────────────────────────┐
│  SQLite Database                │
│  - Stocke les données           │
│  - Exécute les requêtes         │
└──────┬──────────────────────────┘
       │
       │ Result
       ▼
┌─────────────────────────────────┐
│  Prisma ORM                     │
│  - Parse les résultats          │
│  - Type les données (TypeScript)│
└──────┬──────────────────────────┘
       │
       │ Typed Data
       ▼
┌─────────────────────────────────┐
│  Route Handler                  │
│  - Formate la réponse           │
│  - Retourne JSON                │
└─────────────────────────────────┘
```

---

## 🔄 Flux de Cache

```
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ GET /api/trade/sets
       ▼
┌─────────────────────────────────┐
│  Backend - Route /trade/sets    │
└──────┬──────────────────────────┘
       │
       │ Cache Key: "sets:fr"
       ▼
┌─────────────────────────────────┐
│  Cache Memory (Map)             │
└──────┬──────────────────────────┘
       │
       ├─► Cache Hit ?
       │   ├─► Oui → Retourne les données
       │   └─► Non → Continue
       │
       ▼
┌─────────────────────────────────┐
│  TCGdx API (External)           │
│  https://api.tcgdx.net/v2/fr/   │
└──────┬──────────────────────────┘
       │
       │ Fetch Data
       ▼
┌─────────────────────────────────┐
│  Backend                        │
│  - Transforme les données       │
│  - Filtre les séries            │
│  - Stocke dans le cache (60s)   │
└──────┬──────────────────────────┘
       │
       │ 200 OK
       ▼
┌─────────────┐
│  Frontend   │
└─────────────┘
```

---

## 📦 Flux de Gestion du Stock

```
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ POST /api/checkout/create-session
       │ { items: [{ variantId: "abc", quantity: 2 }] }
       ▼
┌─────────────────────────────────┐
│  Backend - Vérification Stock   │
└──────┬──────────────────────────┘
       │
       ├─► Trouve le variant (Prisma)
       │   └─► variant.stock = 10
       ├─► Vérifie : stock >= quantity ?
       │   └─► 10 >= 2 ? ✅ Oui
       └─► Continue...
       │
       │ Crée session Stripe
       ▼
┌─────────────────────────────────┐
│  Stripe Checkout                │
└──────┬──────────────────────────┘
       │
       │ (Temps écoulé...)
       │ (Un autre utilisateur achète 9 unités)
       │
       ▼
┌─────────────────────────────────┐
│  Stock actuel : 1 unité         │
└─────────────────────────────────┘
       │
       │ Webhook Stripe
       ▼
┌─────────────────────────────────┐
│  Backend - Webhook Handler      │
└──────┬──────────────────────────┘
       │
       ├─► Démarre transaction Prisma
       ├─► Re-vérifie le stock
       │   └─► Stock actuel : 1
       │   └─► Quantité demandée : 2
       │   └─► 1 >= 2 ? ❌ Non
       ├─► updateMany retourne count = 0
       └─► Transaction échoue
       │
       │ Erreur : Stock insuffisant
       ▼
┌─────────────────────────────────┐
│  Stripe retente automatiquement │
│  (si erreur 5xx)                │
└─────────────────────────────────┘
```

**Note** : La décrémentation atomique garantit qu'il n'y a pas de race condition. Si deux utilisateurs achètent simultanément le dernier article, un seul réussira.

---

## 🔐 Flux de Validation JWT

```
┌─────────────┐
│  Frontend   │
└──────┬──────┘
       │
       │ Header: Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
       ▼
┌─────────────────────────────────┐
│  Middleware authenticateToken   │
└──────┬──────────────────────────┘
       │
       ├─► Extrait le token du header
       │   └─► "Bearer eyJhbGc..." → "eyJhbGc..."
       ├─► Vérifie la signature
       │   └─► Utilise JWT_SECRET
       │   └─► jwt.verify(token, secret)
       ├─► Vérifie l'expiration
       │   └─► exp: 1234567890 < Date.now() ?
       ├─► Parse le payload
       │   └─► { userId, email, username, isAdmin }
       └─► Ajoute à req.user
       │
       │ Token valide
       ▼
┌─────────────────────────────────┐
│  Route Handler                  │
│  - Accède à req.user            │
│  - Utilise req.user.userId      │
└─────────────────────────────────┘
```

**Structure du JWT** :
```
Header: { alg: "HS256", typ: "JWT" }
Payload: { userId: "abc123", email: "user@example.com", isAdmin: false, exp: 1234567890 }
Signature: HMACSHA256(base64UrlEncode(header) + "." + base64UrlEncode(payload), secret)
```

---

## 📊 Résumé des Flux Principaux

### 1. Authentification
```
Register/Login → Hash Password → Generate Tokens → Store Refresh Token → Return Tokens
```

### 2. Achat
```
Get Product → Check Stock → Create Stripe Session → User Pays → Webhook → Update Stock → Create Order
```

### 3. Sécurité
```
Request → CORS → Helmet → Validate → Sanitize → Injection Protection → Rate Limit → Route
```

### 4. Base de Données
```
Route → Prisma → SQL → SQLite → Result → Prisma → Typed Data → Route → JSON Response
```

---

**Documentation créée le :** 2025-01-09
**Version :** 1.0.0

