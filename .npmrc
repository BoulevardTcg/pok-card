# Monorepo root is not used for app installs anymore.
# Prevent creating/updating a lockfile at the root by mistake.
package-lock=false

# Optional quality-of-life settings (noisy in CI/local)
fund=false
audit=false
